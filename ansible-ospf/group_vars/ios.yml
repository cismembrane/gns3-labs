ospf_process_id: 1
ospf_area: 0

exclude_mgmt_from_ospf: true

ospf_validation_commands:
  - show ip ospf neighbor
  - show ip route ospf
  - show ip protocols

base_config:
  domain_name: test.com
  ssh_modulus_bits: 1024
  mgmt_username: admin
  mgmt_password: admin
  enable_secret: admin
