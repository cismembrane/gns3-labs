Execution of ansible-playbook playbooks/interface-config.yml -vv shows that no switchport is timing out.

--

Made changes to ansible.cfg, attempting to get no switchport to work. connect_timeout to 60 and command_timeout to 120.

[persistent_connection]
connect_timeout = 60
command_timeout = 120

---

I executed ansible-playbook playbooks/interface-config.yml -vv with the new timeout values. I have now lost connectivity beyond R2, which is the node facing the cloud interface.

--

DTP flap observed on R1, port err-disabled:

R1#show int status

Port      Name               Status       Vlan       Duplex  Speed Type 
Gi0/2                        notconnect   1            auto   auto unknown
Gi0/3                        notconnect   1            auto   auto unknown
Gi0/0                        connected    routed       auto   auto unknown
Gi0/1                        connected    1            auto   auto unknown
Gi1/0                        err-disabled 1            auto   auto unknown
Gi1/1                        notconnect   1            auto   auto unknown
R1#conf t
Enter configuration commands, one per line.  End with CNTL/Z.
R1(config)#
R1(config)#end 
R1#
R1#show int st
*Mar  5 01:28:15.801: %SYS-5-CONFIG_I: Configured from console by console
R1#show int status err
R1#show int status err-disabled 

Port      Name               Status       Reason               Err-disabled Vlans
Gi1/0                        err-disabled dtp-flap

Interface G1/0 has been shut/no shut in an attempt to fix the problem.

--

Even with the interface back up, the connectivity problem remains. I believe it is due to the lack of a L2 port between R2 and other routers.

I'm going to need dedicated L2 management ports to do this.

A dedicated L2 port has been added between each node.

--

After running ansible-playbook playbooks/interface-config.yml -vv again, I discovered R1 and R4 are attempting to assign IP addresses to nonexistent interfaces on the connection between R1 and R4. The interface names have been changed to the correct interface, G1/0, instead of G0/4.

--

ansible-playbook playbooks/interface-config.yml -vv yields a timeout on R4.

Ansible is not pushing the interface IPs. Looking into it.

Added the following to interface-config.yml to show the interface IPs:
	- name: Show intended interface config for this host
  ansible.builtin.debug:
    var: l3_interfaces

Running ansible-playbook -i inventory-ios.ini playbooks/interface-config.yml -l R1 -vvv

I've got a stability problem with viosvl2. It does not like assigning IP addresses to physical interfaces. I'm going to rewrite the appropriate files to assign IP addresses to SVIs.

VLAN 10 assigned to 10.0.1.0/30, VLAN 20 assigned to 10.0.2.0/30, VLAN 30 assigned to 10.0.3.0/30, and VLAN 4 assigned to 10.0.4.0/30

I've modified everything accordingly.

The iosvl2 image on R1 is experiencing a crash when Ansible executes default int g0/1. I am deleting the entire section that resets ports due to this.

I've added a section to disable DNS lookup in base-config.yml.

R1 is getting hung on the switchport command. I'm removing it from interface-config.yml.

I'm still not having any luck with viosl2. It keeps randomly crashing on the no switchport command. I'm redesigning this for vios images.

vios is both processor intensive and unstable on this machine. I'm looking into other images.

I've settled on c3660 router images running on dynamips in GNS3.

Ubuntu REALLY doesn't like SSH on the c3660. I'm trying a 7200 router image now.

I'm having good success with the c7200 images. I'm refactoring the current Ansible files to use physical interfaces as opposed to SVIs.

Ansible files taken care of. I just tried ansible-playbook -i inventory-ios.ini playbooks/interface-config.yml -l R1 --diff and discovered that some of the interfaces are mislabeled. Changed.






