# Override Grafana admin credentials by copying this file to .env
# cp .env.example .env
# Then edit .env with your chosen values.
# If .env does not exist, compose falls back to admin/admin as defined in docker-compose.yml.

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

